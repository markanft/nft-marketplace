export type NFT = {
  price: string;
  tokenId: number;
  seller: string;
  owner: string;
  image: string;
  name: string;
  description: string;
  sold:boolean;
};

export type NftMeta = {
  name: string;
  decscription: string;
  image: string;
};
